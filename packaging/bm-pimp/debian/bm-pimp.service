[Unit]
Description=BlueMind system configuration PIMP
PartOf=bluemind.target

[Service]
Type=oneshot
ExecStart=/etc/init.d/bm-pimp start
RemainAfterExit=yes
# 0 is the same as infinity, but "infinity" needs systemd 229
TimeoutStartSec=1h

[Install]
WantedBy=bluemind.target
