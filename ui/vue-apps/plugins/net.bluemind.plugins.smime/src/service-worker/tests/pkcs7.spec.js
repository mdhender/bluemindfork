import pkcs7 from "../pkcs7";

describe("pkcs7", () => {
    describe("decrypt", () => {
        test("decrypt pkc7 part if the right private key is given", () => {});
        test("raise an error if the private key is wrong", () => {});
        test("select the right recipient if multiple recipient are present", () => {});
        test("raise an error if the given certificate does not match any recipient", () => {});
    });
});
