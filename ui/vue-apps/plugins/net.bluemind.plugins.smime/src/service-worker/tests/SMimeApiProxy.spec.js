import SMimeApiProxy from "../SMimeApiProxy";

describe("SMimeApiProxy", () => {
    describe("multipleGetById", () => {
        test("decrypt encrypted messages ", () => {});
        test("do nothing if messages are not encrypted ", () => {});
        test("call next to get messages meta data", () => {});
    });
});
