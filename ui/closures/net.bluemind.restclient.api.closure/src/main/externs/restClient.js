/**
 * @fileoverview RestClient api
 * 
 * @externs
 */

/** @const */
var restClient = {};



/**
 * @param {object} request
 * @param {function()} replyHandler
 */
restClient.sendMessage = function(request, replyHandler) {
};

/**
 * @param {function()} onlineStatelistener
 */
restClient.addListener = function(onlineStatelistener) {
};

/**
 * @param {function()} onlineStatelistener
 * @return {boolean}
 */
restClient.online = function() {
};